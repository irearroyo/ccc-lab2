# AWS Configuration
aws_region = "us-east-1"
environment = "dev"

# VPC Configuration (IMPORTANT: Change this to avoid overlap with partner's VPC)
vpc_cidr = "10.0.0.0/24"
availability_zone = "us-east-1a"

# S3 Bucket Configuration (IMPORTANT: Change this to make it globally unique)
s3_bucket_suffix = "your-initials-random"
